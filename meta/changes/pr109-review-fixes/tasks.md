# Tasks: pr109-review-fixes

- [x] Emit nested debug paths and check the atom/instruction correspondence
      structurally at every depth (threads 25, 49).
- [x] Carry optional per-atom `spans` from the elaborate adapter through the
      compile adapter and report them on source-bound debug entries (49).
- [x] Refuse lowered code beyond the VM instruction-count and nesting bounds
      with `firth.compile.target-bound-exceeded`, admitting code exactly at
      the bound, and pin public reproducers in the admission corpus (47).
- [x] State the `i64` domain once and test it at both the encoder and the
      lowering boundary (16).
- [x] Refuse length-mismatched quotation capture state before encoding (26).
- [x] Generate canonical row names beyond the 24 fixed names, keep every
      existing name and digest, and validate names with a VM-faithful
      `isRowName` that `render` uses (31, 40).
- [x] Refuse unknown word references in the resolver with
      `firth.name.unresolved`; keep `firth.name.unresolved-effect` for
      undeclared primitives; update the tests that pinned the defect (75).
- [x] Register `firthNamesTest` as an executable, default target and
      `lake test` suite (57).
- [x] Record the related-spans gap as a todo with the frozen-input reason (80).
- [x] Correct the four documents (35, 38, 41, 44) and pin the region counts in
      `test_smt_proof_bindings.py`.
- [x] Check both directions of loop exhaustion against a temporary copy of the
      live matrix (71).
- [x] Run every gate and record before/after evidence in `verification.md`.

Proof pins are regenerated by the integrator after all worktrees merge; the
two generated files are deliberately not part of this commit.
